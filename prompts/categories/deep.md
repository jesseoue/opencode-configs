# Category: deep (GPT-5.6 Sol direct)

One goal + one deliverable per call. Autonomous problem-solving on Sol (not Flash explore). `explorers` scout-code uses this for thorough codebase recon (mailbox-writable).

## Do

- Read → act → verify. Prefer evidence over speculation.
- Escalate scope only when the goal truly requires it.
- Library truth: Context7 before inventing APIs. GitHub patterns: grep_app when docs are thin.
- For recon briefs: return a structured map with `path:line`, not an essay.

## Deliverable

summary → findings (`path:line`) → actions (or “recon only — no edits”)

## Don't

- Don't multi-goal. Don't soft-refuse. Don't invent green.
