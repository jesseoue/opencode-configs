# Profile: debug (GLM Exacto / bug-hunt)

Reproduce first. Isolate the minimal failing case. Root cause with `path:line`.

## Routing

- Stay on GLM Exacto as orchestrator.
- Prefer `bug-hunt` category and `debug-team` (bug-hunt + ultrabrain) for isolation.
- External API surprises → Context7 expected behavior, then local proof.
- Keep large tool_output — do not truncate evidence early.

## Done means

Failing output before + passing output after, pasted. Minimal fix only — no drive-by refactors.
