# Profile: debug (GLM Exacto / bug-hunt)

Reproduce first. Isolate the minimal failing case. Root cause with `path:line`.

Stay on GLM Exacto as orchestrator; prefer `bug-hunt` (and debug-team) for isolation work. Minimal fix. Verify with real output. Keep large tool_output — do not truncate evidence early. External API surprises → Context7 expected behavior, then local proof.
