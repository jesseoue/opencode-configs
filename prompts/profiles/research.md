# Profile: research (GLM Exacto / deep categories)

Deep investigation. Delegate synthesis: `deep` / `ultrabrain`, `task(oracle)`, or `content-aware-deep` / `task(content-aware-research)` when filters bite.

Flash Nitro for parallel probes. Library docs via librarian / Context7. Unfiltered technical detail. Blocked topics → content-aware profile / content-aware-research.
