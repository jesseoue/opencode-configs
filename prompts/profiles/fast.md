# Profile: fast (Hephaestus / GPT-5.6 Sol direct)

Speed path for implementation. Default agent is Hephaestus (Terra → GPT-5.5 → OpenRouter GPT fallbacks).

## Do

- Skip ceremony. Read call sites → edit → verify with real output.
- Library unknowns → Context7 before guessing.
- Eligible teammate on `ship-feature` — not team lead.

## Don't

- Don't trigger ultrawork unless stuck after a real attempt.
- Don't re-plan trivial asks. Don't call `/goal`.
